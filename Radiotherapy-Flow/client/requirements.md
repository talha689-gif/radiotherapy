## Packages
date-fns | Required for robust date formatting in the medical dashboard

## Notes
- Using Shadcn UI components for the interface (installed in base stack)
- Dates are passed as ISO strings to the backend and coerced/handled by Drizzle
- The dashboard is designed as a single-page application with robust modals to match the requested workflow
